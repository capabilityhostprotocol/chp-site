{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "git config --global url.\"https://${GH_PKG_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && git submodule update --init --recursive && rm -rf node_modules/@chp/ui && npm install"
}
