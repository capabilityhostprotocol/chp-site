{
  "name": "chp-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:adapters": "node scripts/sync-adapters.mjs",
    "sync:capabilities": "node scripts/sync-capabilities.mjs",
    "sync": "node scripts/sync-adapters.mjs && node scripts/sync-capabilities.mjs",
    "predev": "npm run sync",
    "dev": "next dev",
    "prebuild": "npm run sync",
    "build": "next build",
    "verify": "npm run sync && NEXT_DIST_DIR=.next-verify next build",
    "start": "next start",
    "test:routes": "node scripts/verify-routes.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "^15.3.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10",
    "postcss": "^8",
    "tailwindcss": "^3.4.17",
    "typescript": "^5"
  }
}
